bundle:
  name: db-residential-investment-copilot

include:
  - resources/*.yml

sync:
  include:
    - src/app/.build

artifacts:
  app:
    build: bash build.sh
    path: src/app

variables:
  catalog:
    default: "startups_catalog"
  raw_schema:
    default: "dbx_res_raw"
  bronze_schema:
    default: "dbx_res_bronze"
  silver_schema:
    default: "dbx_res_silver"
  gold_schema:
    default: "dbx_res_gold"
  warehouse_id:
    lookup:
      warehouse: "Serverless Starter Warehouse"

targets:
  dev:
    default: true
    mode: development
    workspace:
      profile: vm
    variables:
      catalog: "startups_catalog"

  prod:
    mode: production
    workspace:
      profile: vm
    variables:
      catalog: "startups_catalog"
