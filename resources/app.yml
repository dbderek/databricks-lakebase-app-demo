resources:
  apps:
    db_residential_copilot_app:
      name: "db-residential-copilot-${bundle.target}"
      description: "Databricks Residential Investment Copilot"
      source_code_path: ../src/app/.build
      # Database resource is added via deploy.sh after both the app
      # and Lakebase project are provisioned (Autoscale projects
      # aren't visible as "database instances" until they exist).
